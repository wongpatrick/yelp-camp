<% include partials/header %>

<h1>This is the campgrounds page!</h1>

<ul>
    <% campgrounds.forEach(function(campground){ %>
        <div>
            <h4><%= campground.name %></h4>
            <img src="<%= campground.image %>" style="width=100px">
        </div>
    <% }) %>
</ul>

<% include partials/footer %>